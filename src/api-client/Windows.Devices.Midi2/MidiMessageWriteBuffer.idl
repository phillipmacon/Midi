// Current level: Prototype

// TODO consider making this a built-in stream type like IOutputStream
// THis is a TEMPORARY interface and is not based on Gary's MIDI buffer work. That work will replace this.

namespace Windows.Devices.Midi2
{
	[default_interface]
	runtimeclass MidiMessageWriteBuffer
	{
		MidiMessageWriteBuffer();

		Boolean CanWrite(UInt16 numberOfWords);

		void WriteWord(UInt32 word);
		void WriteWords(UInt32[] words);


		// TODO
	}
}